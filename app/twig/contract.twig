{% extends "page.twig" %}

{% block title %}{{ contract.display_name }} — {{ 'nav.contracts'|trans }}{% endblock %}

{% block breadcrumbs %}
        <li><a href="/" aria-current="page">{{ 'common.short_name'|trans }}</a></li>
        <li><a href="/contracts/" aria-current="page">{{ 'nav.contracts'|trans }}</a></li>
        <li class="is-active"><a href="#" aria-current="page">{{ contract.display_name }}</a></li>
{% endblock %}

{% block content %}
        <script>
                $(function () {
                        $(".table").tablesorter();
                });
        </script>
<h1 class="title">{{ 'contract_page.header'|trans }}: {{ contract.display_name }}</h1>
<h1 class="subtitle">
        <span class="is-family-monospace">{{ contract.hash }}</span>
</h1>


{% if contract.url %}
<p class="block"><a href="{{ contract.url }}">{{ 'contract_page.link'|trans }}</a></p>
{% endif %}
{% if contract.text %}
<p class="block"><a href="/contracts/{{ contract.hash }}/text">{{ 'contract_page.link_to_text'|trans }}</a></p>
{% endif %}

<table class="table" data-sortlist="[[0,0],[1,0]]">
        <thead>
        <tr>
                <th data-sorter="text">{{ 'contract_page.account'|trans }}</th>
                <th data-sorter="text">{{ 'contract_page.signature_name'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for signature in signatures %}
        <tr>
                <td>{{ include('account_link.twig', {data: signature.account}, with_context = false) }}</td>
                <td>{{ signature.name }}</td>
        </tr>
        {% endfor %}
        </tbody>
</table>

{% endblock %}
